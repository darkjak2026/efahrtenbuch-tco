# Projekt: eFahrtenbuch⚡TCO — Umzug auf Vercel (Next.js + Upstash Redis)

**App-Name:** eFahrtenbuch⚡TCO, Unterzeile: "Total Cost of Ownership für B10 & T03"
(Titel-Tag und Header-Überschrift entsprechend setzen, siehe Referenzdatei
`2026-07-05_10.22_eFahrtenbuchTCO_V2.11.html`)

## Ausgangslage

Es gibt bereits eine funktionierende, aber rein lokale Version als einzelne HTML-Datei
(`2026-07-05_10.32_eFahrtenbuchTCO_V3.00.html`). Sie läuft im Browser und speichert
alle Daten in `localStorage`. Das Problem: Zwei Nutzer (Jakob & Anja) auf zwei
verschiedenen Geräten sehen dadurch nicht denselben Stand.

**Ziel dieses Projekts:** Dieselbe App als Next.js-Anwendung auf Vercel deployen, mit
einem echten Online-Speicher (Upstash Redis über den Vercel Marketplace), sodass beide
Nutzer live denselben Datenstand sehen — unabhängig vom Gerät.

## Funktionsumfang (1:1 aus der bestehenden Datei übernehmen)

1. **Ladeprotokoll**: Monats-Reiter Juli–Dezember 2026. Pro Zeile: Datum, Fahrzeug
   (Dropdown: B10 / T03), Ladekarte (Dropdown: EWE Go, InCharge, Aral pulse,
   EnBW HyperNetz, Chargemap), Ladestation (Text, optional per GPS befüllt), Dauer
   (hh:mm), kWh, Preis (€), km-Stand (Kilometerzähler zum Zeitpunkt des Ladens).
   Kumulierte kWh/€-Spalten pro Zeile, Summenzeile pro Monat.
2. **GPS-Lookup**: Button pro Zeile, ruft `navigator.geolocation.getCurrentPosition`
   auf, fragt danach die öffentliche **Open Charge Map API**
   (`https://api.openchargemap.io/v3/poi/?output=json&latitude={lat}&longitude={lon}&maxresults=1&distance=0.6&distanceunit=KM`)
   ab und trägt den Namen der nächsten Ladestation automatisch ein (editierbar).
   Fällt zurück auf reine Koordinaten, wenn kein Treffer da ist.
3. **TCO-Übersicht** (Total Cost of Ownership), oben prominent, drei Karten:
   - Leapmotor B10, Leapmotor T03, Haushalt gesamt
   - Je Karte: **€/km** (großer Wert) + TCO gesamt + Aufschlüsselung
     (Ladekosten, Leasing+Versicherung, wiederkehrende Kosten, Investitionen;
     bei Haushalt die haushaltsweiten wiederkehrenden Kosten statt fahrzeugspezifischer)
   - km-Berechnung pro Fahrzeug: `max(km-Stand) − min(km-Stand)` über alle erfassten
     Ladevorgänge dieses Fahrzeugs (erst ab 2 Werten pro Fahrzeug)
4. **Fixkosten & Investitionen** (bewusst weniger prominent, in einem eingeklappten
   Bereich `<details>`):
   - Je Fahrzeug: Leasingrate €/Monat, Versicherung €/Monat, Übergabedatum
   - Wiederkehrende Kosten: siehe Punkt 7 unten (ersetzt eine frühere, starre
     Abo-Tabelle durch eine frei erweiterbare Liste)
   - Investitionen/Ausstattung: freie Liste (Datum, Fahrzeug oder "Haushalt",
     Beschreibung, Betrag) — Einmalkosten
   - Ein Datum "Erfassungsstart" für die Abo-Hochrechnung
5. **Export**: CSV pro Monat, komplettes JSON-Backup (Download + Upload zum Wiederherstellen).
6. **Erweiterbare Ladekarten & Tarife**: Die Liste der Ladekarten/Lademöglichkeiten ist
   nutzerseitig editierbar (hinzufügen/entfernen), nicht fest im Code verdrahtet.
   Optional lässt sich pro Karte ein €/kWh-Tarif hinterlegen (z.B. für "Zuhause"); ist
   ein Tarif gesetzt und das Preisfeld einer Zeile noch leer, wird beim Eintragen von
   kWh automatisch ein Preisvorschlag berechnet (überschreibt nie einen bereits
   vorhandenen manuellen Wert).
7. **Generische wiederkehrende Kosten**: Statt einer festen Abo-Tabelle gibt es eine
   frei erweiterbare Liste (Beschreibung, Fahrzeug oder "Haushalt", Betrag/Monat,
   Start-Datum) — deckt Ladekarten-Abos, Stromtarif-Grundgebühren, Apps, finanzierte
   Wallbox-Raten usw. ab. Fließt vehicle-/household-spezifisch in TCO und €/km ein.
   Investitionen (Einmalkosten, z.B. Wallbox-Kauf) bleiben wie bisher eine separate
   freie Liste. Alle Einträge (Ladeprotokoll, Ladekarten, wiederkehrende Kosten,
   Investitionen) bleiben jederzeit editier- und löschbar.
8. **Export: Excel & PDF, monatliche Kontenübersicht**:
   - PDF-Export "Kontenübersicht" für den gerade aktiven Monat: Tabelle aller
     Ladevorgänge dieses Monats + Zusammenfassung (kWh, Ladekosten, anteilige
     Fixkosten/wiederkehrende Kosten mit Einzelposten, Investitionen des Monats,
     Gesamtkosten Monat, gefahrene km je Fahrzeug, €/km Haushalt).
   - Excel-Export "Gesamtexport": ein Workbook mit einem Sheet pro Monat
     (Rohdaten der Ladevorgänge), einem Sheet "Kontenübersicht" (eine Zeile pro
     Monat mit denselben Kennzahlen wie im PDF) und einem Sheet "TCO Gesamt"
     (kumulierte Werte je Fahrzeug + Haushalt).
   - km-Berechnung pro Monat: letzter bekannter km-Stand eines Fahrzeugs bis
     Monatsende minus letzter bekannter km-Stand bis zum Ende des Vormonats
     (fortlaufende Differenzen über Monatsgrenzen hinweg, nicht nur Werte
     innerhalb des einzelnen Monats).
   - Fixkosten/wiederkehrende Kosten werden im jeweiligen Monat voll angesetzt,
     sobald ihr Startdatum erreicht ist (keine Tages-Proration im Startmonat —
     bewusste Vereinfachung, im Export als Hinweis vermerkt).
   - Bibliotheken: **SheetJS (xlsx)** für Excel, **jsPDF + jspdf-autotable** für PDF,
     beide clientseitig per CDN eingebunden (kein Server-Rendering nötig).
9. **Responsives Layout**: Header schmal (kompakte Padding-Werte, `clamp()` für
   Schriftgrößen), TCO-Karten und Fixkosten-Grid nutzen `grid-template-columns:
   repeat(auto-fit, minmax(...))` und brechen automatisch auf kleinere Bildschirme um.
   Die Haupttabelle (Ladeprotokoll) wechselt unterhalb von 720px Breite von
   klassischer Tabellenansicht zu gestapelten Karten pro Zeile (jede Zelle zeigt
   ihr Spalten-Label über `data-label`-Attribute + CSS `::before`), damit auch auf
   dem Smartphone nichts horizontal gescrollt werden muss.

## Tech-Stack

- **Next.js** (App Router), TypeScript
- **Upstash Redis** über den **Vercel Marketplace** als einziger Datenspeicher — ein
  JSON-Dokument unter dem Key `ladeprotokoll:2026`
  *(Hinweis: „Vercel KV" als eigenständiges Produkt wurde von Vercel eingestellt und
  automatisch zu Upstash Redis migriert. Für neue Projekte installiert man die
  Upstash-Redis-Integration direkt aus dem Vercel Marketplace.)*
- Deployment auf **Vercel**, gleiches Konto wie die Redis-Instanz

## Datenmodell (identisch zur bestehenden `localStorage`-Struktur)

```json
{
  "cardsList": ["EWE Go", "InCharge", "Aral pulse", "EnBW HyperNetz", "Chargemap", "Zuhause"],
  "cardTarife": { "Zuhause": 0.32 },
  "vehicles": {
    "b10": { "leasing": 331.51, "versicherung": "", "start": "" },
    "t03": { "leasing": 149.00, "versicherung": "", "start": "" }
  },
  "recurringCosts": [ { "beschreibung": "", "fahrzeug": "", "betrag": "", "start": "" } ],
  "erfassungStart": "2026-07-01",
  "investitionen": [ { "datum": "", "fahrzeug": "", "beschreibung": "", "betrag": "" } ],
  "months": {
    "2026-07": [ { "datum":"", "fahrzeug":"", "karte":"", "ladestation":"", "lat":"", "lon":"", "dauer":"", "kwh":"", "preis":"", "km":"" } ],
    "2026-08": [], "2026-09": [], "2026-10": [], "2026-11": [], "2026-12": []
  }
}
```

## API-Routen

- `GET /api/data` → liest das aktuelle JSON-Dokument aus Upstash Redis, gibt es zurück.
  Existiert noch keines, das Default-Objekt (siehe oben) zurückgeben und gleichzeitig
  in Redis anlegen.
- `POST /api/data` → nimmt das komplette JSON-Dokument im Body, überschreibt den
  Eintrag in Redis. Kein Konfliktmanagement nötig — bei zwei Haushaltsmitgliedern reicht
  "letzter Schreibzugriff gewinnt".
- Beide Routen prüfen einen einfachen Zugriffsschutz: Header `x-pin` oder Cookie muss
  mit der Environment-Variable `LADEPROTOKOLL_PIN` übereinstimmen. Bei Fehlschlag
  `401` zurückgeben.

## Frontend-Verhalten

- Beim ersten Aufruf: PIN-Eingabe-Dialog, PIN in `localStorage` merken (nicht im Klartext
  in der URL).
- Beim Laden der Seite: `GET /api/data`, State daraus befüllen.
- Bei jeder Änderung: **debounced** `POST /api/data` (ca. 800 ms nach der letzten
  Eingabe), damit nicht bei jedem Tastenanschlag ein Request rausgeht.
- Restliche UI-Logik (Tabs, Rendering, Berechnungen für TCO/€/km, GPS-Lookup,
  CSV/JSON-Export) so übernehmen wie in der bestehenden HTML-Datei — dort sind alle
  Formeln bereits fertig implementiert (siehe `renderTco`, `vehicleStats`,
  `householdStats`, `locateStation` in der Referenzdatei).

## Design-Vorgaben (aus bestehender Datei übernehmen, nicht neu erfinden)

- Farben: Plum `#4B3868` / Plum-deep `#352849` / Glacier `#6FA8C7` / Glacier-deep
  `#3E7A9B` / Lavendel-Hintergrund `#F0EDF5` / Warnrot `#B3452F`
- Schriften: **Space Grotesk** (Überschriften), **Inter** (UI/Fließtext),
  **JetBrains Mono** (alle Zahlenwerte)
- Layout: Header-Banner mit Farbverlauf, TCO-Karten-Grid, Monats-Tabs, Tabelle mit
  horizontalem Scroll, eingeklappter Fixkosten-Bereich

## Header-Grafik (wird separat nachgeliefert)

Jakob erstellt die Header-Grafik (B10 + T03 freigestellt, Ladesäule, Smartphone) selbst
in Affinity Designer/Photo. Claude Code baut den Header deshalb so, dass ein Bild
später einfach eingesetzt werden kann, ohne Code-Änderungen nötig zu machen:

- Erwarteter Pfad: `public/header-banner.png` (oder `.webp`), sehr breites Format
  (z.B. 1800×300px, Seitenverhältnis ~6:1)
- Solange die Datei fehlt, bleibt der aktuelle Farbverlauf (Plum → Glacier) als
  Fallback-Hintergrund sichtbar — kein kaputtes Layout, falls das Bild noch nicht da ist
- Einbindung z.B. per CSS: `background-image: url('/header-banner.png')` auf dem
  Header-Container, mit `background-size: cover` und dem Farbverlauf als
  `background-color`-Fallback darunter

## Schritte für Claude Code

1. `npx create-next-app@latest efahrtenbuch-tco --typescript --tailwind --eslint --app`
2. `vercel link` (Projekt mit Vercel-Account verbinden)
3. Upstash-Redis-Integration aus dem **Vercel Marketplace** hinzufügen und mit dem
   Projekt verbinden (Dashboard: Storage → Marketplace Database Integrations →
   Upstash Redis, oder per CLI: `vercel install upstash` bzw. `vercel integration add`).
   Ein eigenständiges „Vercel KV" gibt es nicht mehr zu erstellen.
4. `vercel env pull .env.local` (zieht die von Upstash gesetzten Variablen,
   typischerweise `UPSTASH_REDIS_REST_URL` und `UPSTASH_REDIS_REST_TOKEN`)
5. Zusätzliche Env-Variable `LADEPROTOKOLL_PIN` in Vercel setzen (Dashboard oder
   `vercel env add`)
6. API-Routen unter `app/api/data/route.ts` bauen (GET/POST wie oben beschrieben,
   `@upstash/redis` Paket nutzen, z. B. `Redis.fromEnv()`)
7. Komponenten aufbauen: `TcoPanel`, `FixedCostsPanel`, `MonthTabs`, `ChargeTable`,
   `PinGate`
8. GPS-Lookup-Funktion (`navigator.geolocation` + `fetch` gegen Open Charge Map)
   unverändert übernehmen
9. Lokal testen: `npm run dev`
10. Deployen: `vercel --prod`

## Autonomes Durchlaufen bei Claude Code

Claude Code fragt standardmäßig vor jeder Datei-Änderung und jedem Befehl nach
Bestätigung. Für dieses überschaubare, klar abgegrenzte Projekt kann das mit dem Flag
`--dangerously-skip-permissions` übersprungen werden, damit Claude Code ohne ständige
Rückfragen durchläuft:

```
claude --dangerously-skip-permissions
```

**Wichtig, bewusst nicht beschönigt:** Der Name ist Absicht, nicht Übertreibung. In
diesem Modus führt Claude Code Datei-Änderungen, Befehle und Netzwerkzugriffe ohne
jede Bestätigung aus — inklusive potenziell riskanter Aktionen außerhalb des
Projektordners. Anthropics eigene Empfehlung: nur in einer isolierten Umgebung nutzen
(Container/VM) oder zumindest nur in einem Ordner, in dem nichts Wichtiges liegt, das
nicht auch in Git gesichert ist. Für dieses Projekt heißt das konkret:

- Vorher `git init && git add -A && git commit -m "Vor Claude Code Start"`, damit ein
  Rollback jederzeit möglich ist.
- Nicht mit `sudo`/als Root ausführen — das verweigert Claude Code ohnehin.
- Falls unsicher: stattdessen den in Claude Code eingebauten **Auto-Modus** nutzen
  (mit Shift+Tab umschaltbar) — der fragt bei riskanteren Aktionen weiterhin nach,
  läuft bei Routineänderungen aber ähnlich flüssig durch.

## Abnahmekriterien

- [ ] Ohne korrekte PIN ist kein Zugriff auf die Daten möglich
- [ ] Jakob und Anja sehen nach Reload auf ihren jeweiligen Geräten denselben Stand
- [ ] TCO und €/km berechnen sich weiterhin korrekt (Vergleich mit alter HTML-Version)
- [ ] GPS-Lookup funktioniert (HTTPS ist auf Vercel automatisch gegeben)
- [ ] CSV-Export pro Monat funktioniert
- [ ] JSON-Backup lässt sich weiterhin herunterladen (zusätzlich zum Online-Speicher,
      als Notfall-Kopie)
